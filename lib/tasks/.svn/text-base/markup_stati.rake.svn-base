namespace :markup_stati do
    desc "Enables existing markup stati"
    task :set_enabled_markup_stati => :environment do
      @markup_stati = MarkupStatus.find(:all)
      @markup_stati.each do |markup_status|
        markup_status.is_enabled = 1
        markup_status.save
      end
    end
end