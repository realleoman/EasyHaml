namespace :roles do
    desc "Enables existing roles"
    task :set_enabled_roles => :environment do
      @roles = Role.find(:all)
      @roles.each do |role|
        role.is_enabled = 1
        role.save
      end
    end
end